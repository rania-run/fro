name: fro
description: Dart CLI for managing Flutter app versions, build numbers, and releases across environments.
version: 0.1.0
repository: https://github.com/rania-run/fro

environment:
  sdk: ">=3.4.0 <4.0.0"

executables:
  fro:

dependencies:
  args: ^2.5.0
  chalkdart: ">=2.0.9 <4.0.0"
  cli_spin: ^1.0.1
  http: ^1.2.2
  interact: ^2.2.0
  yaml_edit: ^2.2.2

dev_dependencies:
  lints: ">=4.0.0 <7.0.0"
  test: ^1.25.0
